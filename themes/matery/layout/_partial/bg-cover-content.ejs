<div class="row">
    <div class="col s10 offset-s1 m8 offset-m2 l8 offset-l2">
        <div class="brand">
            <div class="title center-align">
                <% if (config.subtitle && config.subtitle.length > 0) { %>
                    <%= config.subtitle %>
                <% } else { %>
                    subtitle
                <% } %>
            </div>

            <div class="description center-align">
                <% if (config.description && config.description.length > 0) { %>
										<%= config.description %>
                <% } else { %>
                    <p id="hitokoto">正在加载一言...</p>
                <% } %>
            </div>

					</div>
    </div>
</div>
<script>
function getRandomInt(max) {
  return Math.floor(Math.random() * Math.floor(max));
}
// 每天切换 banner 图.  Switch banner image every day.
$('.bg-cover').css('background-image', 'url(https://cdn.jsdelivr.net/gh/liuyaanng/Blog_source/blog_source/medias/banner/' + new Date().getDay() + '.jpg)');
$('.bg-cover').css('background-image', 'url(https://cdn.jsdelivr.net/gh/liuyaanng/Blog_source/blog_source/medias/banner/' + getRandomInt(25) + '.jpg)');
// $('.bg-cover').css('background-image', 'url(https://i.loli.net/2020/10/20/sTglFbBW8yxY4dr.jpg)');
</script>

